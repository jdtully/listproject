import React from "react";
import axios from "axios";

export class EditUserRecord extends React.Component {
  constructor(props) {
    super(props);
    this.state = {
      data: [],
      usernumber: null,
      username: null,
      userphone: null
    };
  }
  OnComponentMount = () =>
    axios.get("http://localhost:5000/users/" + id).then(res => {
      this.setState({
        users: res.data.docs,
        username: res.data.docs.username,
        currentusernumber: res.data.total,
        userphone: res.data.docs.userphone,
        userdate: this.state.userdate,
        progress: "ready for entry"
      });
    });
  render() {
    return (
      <div>
        <h1>hello</h1>
      </div>
    );
  }
}
